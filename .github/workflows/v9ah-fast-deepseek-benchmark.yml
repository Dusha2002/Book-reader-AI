name: v9ah-fast-deepseek-benchmark-legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy benchmark notice
        run: |
          echo 'This non-frozen A/B is retained only as a manual legacy entry point.'
          echo 'Use v9ah-frozen-draft-benchmark for deterministic primary-draft comparisons.'
