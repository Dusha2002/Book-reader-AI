name: v9ah-frozen-draft-benchmark-disabled

# Frozen A/B is intentionally retired. Current development validates one real
# chapter end-to-end, then manually audits the entire EN→RU output.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Frozen A/B benchmark is retired; use the current single-chapter audit workflow instead.'
